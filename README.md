# Ads manager
Simple ads manager

# Usage
To render shortcode:

```
[ads id="123"]
```

To render ads:

```
echo ads_code($id_ads);
```

To render position:

```
echo ads_position($slug_position);
```